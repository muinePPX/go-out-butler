# 出门管家机器人 · 板端维护速查手册

> 本文件汇总已排查完成的关键信息，遇到问题先看这里，避免重复排查。
> 最后更新：2026-08-05

---

## 1. 连接板端

| 项 | 值 |
|---|---|
| 板端 IP | `10.45.162.231` |
| SSH | `root@10.45.162.231:2223`（mclaw sshd，密钥免密） |
| hdc | 5555 / 8710 均不通，USB 也未挂载，别用 |
| 系统 | OpenHarmony，Linux 5.10.110 aarch64 |

```sh
# 执行命令
ssh -o ConnectTimeout=10 -o StrictHostKeyChecking=no -p 2223 root@10.45.162.231 "命令"

# 传文件
scp -o ConnectTimeout=10 -o StrictHostKeyChecking=no -P 2223 本地文件 root@10.45.162.231:/tmp/xx
```

**板端 shell 注意**：toybox，无 `bash`（用 `sh`）、无 `python3`（用 `/data/local/release/usr/bin/python3`）、无 `awk`/`tr`。
**Windows PowerShell 注意**：引号转义易坏命令，复杂命令一律 `write_to_file` 写 `.sh` 脚本 → scp 上传 → `ssh ... sh /tmp/xxx.sh` 执行。

---

## 2. 主相机（Orbbec Astra Pro Plus）

- 跑在板端 docker 容器 **`rk3588s-vision`** 内：`roslaunch orbbec_camera astra_pro_plus.launch`
- 正常指标：`/astra_camera/rgb/image_raw` ≈ **29.8 Hz**，`/astra_camera/depth/image_raw` ≈ **29.7 Hz**
- 容器日志应含 `VISION_CAMERA=RUNNING`（`docker logs --tail 15 rk3588s-vision`）

```sh
# 测相机话题（需先加载完整环境）
ssh ... "sh -c '. /tmp/node_full_env.sh 2>/dev/null; timeout 12 rostopic hz /astra_camera/rgb/image_raw'"
```

**完整节点环境 `/tmp/node_full_env.sh`**：共 39 个环境变量（7 个 `LD_LIBRARY_PATH` + `PYTHONHOME` + `PYTHONPATH` 等）。
只用单个 LD_LIBRARY_PATH 会报 `PyType_IsSubtype: symbol not found` / cv2 Signal 11。

**血的教训**：`kill` orbbec 进程会**连带停掉 rk3588s-vision 容器**。恢复用 `docker run` 重建（见 `tmp_start_vision.sh`；脚本内检查 wlan0 IP 的步骤会失败，改用 `127.0.0.1` 手动执行即可）。

---

## 3. dora 数据流

- 数据流文件：板端 `/data/local/robot/dataflow.yml`（本地副本：本目录 `dataflow.yml`）
- 节点链：`voice_input → mclaw_decision → task_scheduler → nav_control / visual_servoing / camera_perception / arm_control / feedback`
- 状态机：`IDLE → NAV → PERCEIVE → ALIGN → GRAB → DONE`
- 关键节点进程：`camera_perception.py`（识别，输出 `item_result`）、`visual_servoing.py`（伺服，输出 `servo_vel` / `servo_result`）

```sh
cd /data/local/robot
run dora up
run dora start dataflow.yml
run dora list          # 看实例与状态
```

**常见故障**：残留旧 dora 实例（如 `hsv_detector_16651`）会抢占相机导致主相机无数据 → `ps -ef | grep dora` 找到后 `kill`，重启 dora 即可。

---

## 4. 机械臂小摄像头（Sonix UVC）

| 项 | 值 |
|---|---|
| 设备 | Sonix Technology USB 2.0 Camera |
| VID:PID | `2bc5:050f` |
| USB 路径 | `/sys/bus/usb/devices/1-1.3.3.2` |

**问题**：默认挂在 `usbfs`，不产生 `/dev/videoN`。需手动绑定到 uvcvideo：

```sh
echo "2bc5 050f" > /sys/bus/usb/drivers/uvcvideo/new_id
# 然后从 usbfs unbind，再 bind 到 uvcvideo
# 绑定成功后产生 /dev/video20 /dev/video21，分辨率 640x480
```

**绑定是临时的**：板端重启后丢失（`/dev/video20` 消失），需重新绑定。判断丢失：`ls /dev/video*` 只有 video0-19，`ls /sys/bus/usb/devices/*/product | grep Sonix` 仍在。
参考脚本：`tmp_probe_uvc.sh`（绑定成功）、`tmp_test_aux_cam.py`（video20 出图验证）。

---

## 5. 当前任务上下文（黄色圆圈抓取）

- **目标**：抓取地面上的**黄色圆圈**（原红色物块已废弃）
- **难点**：场地边缘是黄色胶带线，需区分 → 圆形度过滤 `circularity >= 0.7`
- **遮挡问题**：主相机被机器人自身结构/盲区遮挡 → 用**机械臂小摄像头俯视地面**做黄色圆 RGB 检测
- 厂商 aux 接口（`/data/robot-host/student/`）的 `aux_camera`/`aux_target` 需要深度传感器，Sonix 是 2D 不可直接用，需自己实现 RGB 检测

### 本地已修改文件
| 文件 | 改动 |
|---|---|
| `lib/hsv_detector.py` | 面积上限 8000→30000、MIN_CIRC 0.6→0.7、深度范围 300~1500mm |
| `nodes/camera_perception.py` | SCAN_TIMEOUT 25→18s、SCAN_WZ 0.35→0.45 rad/s、circularity>=0.7 排序 |
| `nodes/visual_servoing.py` | 新增 SEARCH_MAX_SEC=6.0（丢失搜索超时放弃） |
| `nodes/nav_control.py` | `_keepalive_loop` 10Hz 重发指令防底盘超时；target_lost 时 `\|wz\|>0.01` 旋转搜索 |

### 已知硬伤
- `arm_poses.json` 全是 `[0,0,0,0,0]` 零位姿 → 需现场用 `arm_teach.py` 示教标定抓取点位
- 临时诊断脚本（本目录 `tmp_*.sh` / `tmp_*.py`）：`tmp_probe_uvc.sh`、`tmp_test_aux_cam.py`、`tmp_mk_node_env.sh`、`tmp_probe_servo2.py`、`tmp_start_vision.sh`、`tmp_status_check.sh`、`tmp_verify_cam.sh`

---

## 6. 现场排障速查

| 现象 | 排查 | 修复 |
|---|---|---|
| 主相机无话题数据 | `ps -ef \| grep orbbec`；dora 是否残留 | kill 残留 dora 实例，重启容器 |
| cv2 加载报错 | 环境变量不全 | `source /tmp/node_full_env.sh` |
| video20 消失 | `ls /dev/video*` | 重新执行 UVC 绑定（见第 4 节） |
| 机器人乱跑 | 视觉伺服目标丢失 | 检查 SEARCH_MAX_SEC / target_lost 逻辑 |
| 容器被误停 | `docker ps -a` | 按 tmp_start_vision.sh 重建（IP 用 127.0.0.1） |
